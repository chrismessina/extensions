# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules

# Raycast specific files
raycast-env.d.ts
.raycast-swift-build
.swiftpm
compiled_raycast_swift
compiled_raycast_rust

# Central Icon System geometry — PROPRIETARY, never commit.
# Built locally by `npm run build:icons` for a licensed user. The iconists
# license forbids sharing "the Item or its parts publicly on the web"; a public
# repo is exactly that. See docs/research.md §1.
/assets/central-icons.*

# misc
.DS_Store
.github/docs
.claude

# Impeccable design-hook config (local tooling, never ships)
.impeccable/
